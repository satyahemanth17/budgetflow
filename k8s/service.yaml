apiVersion: v1
kind: Service
metadata:
  name: budgetflow-api
  namespace: default
spec:
  selector:
    app: budgetflow-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
